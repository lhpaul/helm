# Secret references resolved by `pnpm sync-env` (1Password CLI `op inject`).
# Local paths and optional overrides live in `.env.local` (see `.env.local.example`).

LINEAR_API_KEY=op://Leasity/leasity-tenants/linear-api-key
GITHUB_WEBHOOK_SECRET=op://Leasity/leasity-tenants/github-webhook-secret
