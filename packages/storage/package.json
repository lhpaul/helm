{
  "name": "@helm/storage",
  "version": "0.0.0",
  "private": true,
  "description": "Filesystem helpers para data/ (agent runs, logs, costs). Se implementa en Sesión 3.",
  "scripts": {
    "build": "echo 'placeholder'",
    "test": "echo 'placeholder'",
    "lint": "echo 'placeholder'"
  }
}
