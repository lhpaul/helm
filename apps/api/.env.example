# Copy this file to .env and fill in the values for local development.
# Bun reads .env from the working directory of the process (apps/api/),
# so this file lives here — NOT in the repo root.

# Absolute path to the knowledge repo for the active Product.
# This repo contains .helm/product.yaml with the Product configuration.
# Example: /Users/yourname/Git/Helm/helm-knowledge
HELM_KNOWLEDGE_REPO_PATH=

# Absolute path to the operational data directory.
# Defaults to 'data/' relative to the process CWD (i.e., apps/api/data/ in dev).
# Contains: items/ (workflow state), agent-runs/, worktrees/, reviewers/, etc.
# Example: /Users/yourname/Git/Helm/helm/apps/api/data
HELM_DATA_DIR=

# GitHub Personal Access Token with project + issues + org:hook scopes.
# Required when the issue_tracker provider is 'github_projects'.
GITHUB_TOKEN=

# Shared secret for validating X-Hub-Signature-256 on incoming GitHub webhooks.
# Must match the secret configured on the webhook in GitHub (or via registerWebhook).
# Required for POST /webhooks/github to accept any payloads.
GITHUB_WEBHOOK_SECRET=
