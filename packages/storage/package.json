{
  "name": "@helm/storage",
  "version": "0.0.0",
  "private": true,
  "description": "Filesystem helpers for data/ (agent runs, logs, costs).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@types/node": "^22.19.19"
  }
}
